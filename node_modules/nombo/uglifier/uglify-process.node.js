var uglify = require('uglify-js');

var simplifyError = function (err) {
	var error;
	if (err.stack) {
		error = {
			message: err.message,
			stack: err.stack
		}
	} else {
		error = err;
	}
	return err;
};

var options = {
	fromString: true,
	mangle: true
};

process.on('message', function (m) {
	if (m.type == 'init') {
		if (m.data.mangle != null) {
			options.mangle = m.data.mangle;
		}
	} else if (m.type == 'minify') {
		var response = {id: m.id};
		try {
			response.type = 'result';
			response.data = uglify.minify(m.data, options).code;
		} catch (err) {
			response.type = 'error';
			response.data = simplifyError(err);
		}
		process.send(response);
	}
});