var conf = require('nombo/configmanager');
var ws = require('nombo/webservice');

var ServerService = new (function () {
	var self = this;
	
	self.getClient = function (port) {
		var host = 'localhost';
		var secure = false;
		return ws.getClient(host, port, secure);
	}
})();

module.exports = ServerService;
